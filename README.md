# Fetching data from jump-cloud

### Installation:

Clone the repo:

    git clone https://github.com/kauz/jump-cloud.git

Install dependencies

    npm i

### Usage:

    ./bin/jcfetch -d <destination>

For example:

    ./bin/jcfetch -d ~/
